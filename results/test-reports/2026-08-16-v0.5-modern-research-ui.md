# v0.5 现代科研画布界面回归记录

日期：2026-08-16
分支：`research/nontrivial-core`

## 变更范围

- 以 AutoFigure-Edit 公开网页为主要视觉参照；
- 平均值 dq 左栏移除研究按钮瀑布，只保留参数、主分析和参数保存；
- 新增三张研究任务卡和随结果启用的导出工具条；
- 保持模型、API、数值结果和报告内容不变。

## 验证结果

| 检查 | 结果 |
|---|---|
| TypeScript 与 Vite 生产构建 | 通过；保留既有大代码包性能提示 |
| Chromium 完整端到端流程 | 最终得到 `BROWSER_E2E_SMOKE_OK` |
| 620 px | 页面宽度等于视口宽度；主区与任务卡均为单列 |
| 900 px | 页面宽度等于视口宽度；主区单列、三张任务卡并排 |
| 1080 px | 左栏320 px、右栏692 px、栏间距24 px；三张任务卡各约223 px |
| 1440/1600 px | 双栏与三任务卡完整，无横向溢出 |

首次完整浏览器回归在等待旧按钮名称“导出可追溯 JSON”时超时。原因是界面已将按钮改名为“结果 JSON”，测试定位器未同步；下载功能本身未报错。同步可访问名称后重跑，论文复现、采样敏感性、同域对照、低频模型、平均值 dq、19点消融、临界边界、42点扫描、JSON导出和报告均通过。

本轮未制作或修改 PPT/PDF，未重新构建 Windows 冻结发布包。
