# v0.5 MathWorks—团队模型八点对齐比较测试记录

## 固定契约

- 基频：`50 Hz`。
- 阻尼换算：`D_team=50D_MW`。
- 网络源阻抗：`|Z|=1/SCR`、`X/R=5`。
- 有功工作点：`P*=0.6、0.8 p.u.`。
- 外部证据：MathWorks 固定模型供应商非线性时域分类。
- 团队证据：16状态平均值 `dq` 模型局部闭环特征根分类。

## 结果

- 八点分类一致：`7/8`。
- 唯一不一致：`SCR=5、D_MW=1.056 p.u./Hz`。
- 团队局部特征根边界：`0.75860001、0.75601169 p.u./Hz` 等效值。
- MathWorks 供应商时域分类区间：`[1.30675,1.32150] p.u./Hz`。
- 定量过渡位置：未复现。

## 产物

- JSON SHA-256：`A66A634813CD946DA10157983B28E0468369BA3915D0B721A5A6E4B6D41C3CEA`。
- CSV SHA-256：`6922603E9757739116017266B771580651113736FFC3BDF38D263AF5149AA205`。
- 对照内核定向测试：`5/5` 通过。
- 前端生产构建：通过。
- 真实 Chromium 端到端流程：通过，八点表、两类边界、边界措辞与 JSON 导出均已核查，输出 `BROWSER_E2E_SMOKE_OK`。
- 发布运行时证据升级为 `gfm-runtime-acceptance/1.5`，固定核对八点中的 `7/8` 一致、唯一不一致点、两类边界及研究范围；异机证据复核保持兼容 `1.2–1.4`。
- 真实生产入口自检：通过，运行时证据状态为 `passed`，输出 `GFM_RELEASE_SMOKE_OK`，服务正常退出并释放端口。
- 后端全量回归：`159/159` 通过，耗时 `241.474 s`；Ruff 静态检查通过。

## 边界

本结果只对齐部分参数坐标。两套模型的内环、限幅、滤波器和分类器仍不同；七点一致不构成完整模型确认，一点不一致也不构成对任一模型的证伪。本轮未制作或修改 PPT/PDF。
